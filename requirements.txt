# Convenience mirror of pyproject.toml dependencies.
# Preferred install: `pip install -e .`  (add `.[remote]` for HuggingFace remote compute)
numpy
tifffile
scikit-image
cellpose
napari[all]
magicgui
qtpy
nd2
# Optional: remote compute via the HuggingFace Cellpose Space
gradio_client
